apiVersion: v1
kind: Service
metadata:
  name: {{ include "ubuntu-w64build.fullname" . }}
  labels:
    {{- include "ubuntu-w64build.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ubuntu-w64build.selectorLabels" . | nindent 4 }}
