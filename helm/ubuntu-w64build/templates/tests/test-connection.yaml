apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ubuntu-w64build.fullname" . }}-test-connection"
  labels:
    {{- include "ubuntu-w64build.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ubuntu-w64build.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
